storm.zookeeper.servers:
  - "%zookeeper%"
storm.zookeeper.port: %zookeeper.port%
nimbus.host: "%nimbus%"
drpc.servers:
  - "%nimbus%"
drpc.port: %nimbus.drcp.port%
drpc.invocations.port: %nimbus.drcpi.port%
